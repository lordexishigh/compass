# ---------------------------------------------------------------------------
# Compass — local defaults. Copy to .env; no secret is required for cold start.
# ---------------------------------------------------------------------------

# Pooled application connection (transaction-mode pooler in production).
DATABASE_URL=postgresql://compass:compass@localhost:5432/compass

# Direct session connection. Migrations, advisory locks and pg-boss maintenance
# MUST use this one: role/session DDL cannot run through a transaction pooler.
DATABASE_URL_DIRECT=postgresql://compass:compass@localhost:5432/compass

# IANA timezone used when a team has no working calendar configured yet. The report
# is dated in this zone, and the window it covers is that zone's previous civil day.
COMPASS_DEFAULT_TIMEZONE=Europe/London

# The team `/` opens on. The seeded organization declares `platform` and `checkout`
# (both scrum) and `insights` (kanban); see seed/MANIFEST.md.
COMPASS_DEFAULT_TEAM=platform

# When the worker generates each team's daily report, as `HH:MM` in *that team's* timezone.
#
# Defaults to 06:00, which is early enough to precede the 07:30 subscription send time so a
# delivery never finds a missing report. Each team's own timezone comes from the roster, so
# this is the local wall-clock time and DST is handled for you. Unset or malformed, 06:00 is
# used. See docs/DEVELOPMENT.md § The scheduled pipeline.
# COMPASS_GENERATION_LOCAL_TIME=06:00

# Narration is optional. Absent, reports render through the deterministic
# template renderer and say so in the page footer — complete, correct, templated.
# Present, Claude writes the six sections from the structured payload only, and
# every number, date, identifier and name it writes is checked against that payload
# before publication. See docs/DEVELOPMENT.md § Narration and grounding.
# ANTHROPIC_API_KEY=

# The narration model. Defaults to claude-sonnet-5 (docs/ARCHITECTURE.md).
# COMPASS_NARRATION_MODEL=claude-sonnet-5

# ---------------------------------------------------------------------------
# The first owner.
#
# Seats are invite-only and only an owner can grant the owner role, so an organization
# with no owner has no path to its first one. The boot script creates this seat on every
# start; it is idempotent and never overwrites a password you have since changed.
#
# Unset, Compass GENERATES a random owner password on the boot that creates the seat, so
# `docker compose up` still gives you an account with nothing configured. It is printed once
# in the boot log and written to `.nous/demo_account.json` (gitignored, mode 0600) — those are
# the only two copies, because the database holds an Argon2id digest and nothing else.
# `/account` says where to find it and cannot show it to you.
#
# There is deliberately no default password in the source. There used to be one — a
# `DEMO_OWNER_PASSWORD` constant in packages/auth/src/bootstrap.ts holding a literal — which meant
# every deployment that forgot this variable shared one password published in this repository.
# The ESLint rule `compass/no-credential-literal` now fails the build on its return.
#
# `/api/health` reports `seats: not_configured` for as long as nothing is configured — a
# deployment on a password its operator never chose must not be able to look healthy.
# See docs/ARCHITECTURE.md § Sessions, tokens and the four-role matrix.
#
# Set BOTH or NEITHER. Each value used to be filled in independently, so setting the email
# and forgetting the password provisioned an owner seat at your real address protected by
# the demonstration password printed below and in the README — configured enough to look
# deliberate, and the weakest of the three possible outcomes. The boot script now refuses
# that combination and names the variable you are missing rather than picking for you.
# ---------------------------------------------------------------------------
# COMPASS_OWNER_EMAIL=you@your-company.com
# COMPASS_OWNER_PASSWORD=at-least-twelve-characters
# COMPASS_OWNER_NAME=Your Name

# The absolute origin Compass puts in a mailed sign-in, reset or invitation link.
#
# Required for any deployment not reached over loopback. It is deliberately NOT taken from
# the request: POST /api/auth/password-reset is public and answers the same for an address
# with a seat and one without, so a caller who could choose the host would choose where
# somebody else's single-use sign-in token pointed. Unset and off localhost, Compass
# refuses to send rather than guess, and says so with a 503 naming this variable.
#
# Unset on localhost — a laptop, or `docker compose up` opened on localhost:3000 — links
# point at that origin, which is what makes the zero-config path work with nothing set.
# COMPASS_BASE_URL=https://compass.your-company.com

# Set when TLS terminates at a proxy that does not send `x-forwarded-proto`. Without it
# the session cookie is issued without `Secure`, and with it over plain HTTP the browser
# would drop the cookie silently. Only one of those is a state you can debug.
# COMPASS_ASSUME_HTTPS=true

# ---------------------------------------------------------------------------
# Delivery: email and Slack.
#
# Both channels are optional and the product is fully usable without either — the
# report is always readable at `/`. With no key configured, a scheduled delivery is
# recorded in `delivery_log` with status `skipped` and a sentence naming the missing
# variable, the worker carries on, and `/api/health` reports the capability as
# not_configured. Nothing is silently dropped and nothing crashes.
# ---------------------------------------------------------------------------

# Resend API key. Also the transport for sign-in links, password resets and
# invitations: until it is set those are written to the process log instead.
# RESEND_API_KEY=

# The verified sending address emails come from, e.g. "Compass <daily@your-company.com>".
# Required alongside RESEND_API_KEY — Resend will not send from an unverified domain.
#
# Configure SPF, DKIM and DMARC on that domain in the Resend dashboard before sending
# to real inboxes; a daily that lands in spam is a daily nobody reads. Compass takes
# this address from configuration and never from a request, so a caller cannot choose
# what a report appears to be sent from.
# COMPASS_EMAIL_FROM=

# Slack bot token (`xoxb-…`), with `chat:write` scope. The same token delivers to a DM
# and to a channel: a subscription's target is a user id (`U…`) for a DM or `#channel`.
# SLACK_BOT_TOKEN=

# Slack signing secret, for verifying the feedback-button callbacks Slack posts back.
#
# Required for Slack feedback buttons to work at all: with this unset, `/api/slack/actions`
# refuses every interaction with a 503 rather than trusting an unverified request. Point
# Slack's interactivity request URL at `<COMPASS_BASE_URL>/api/slack/actions`.
#
# Verification is over the *raw* request body inside a 5-minute timestamp window. A valid
# signature proves the workspace, not the person, so a click is additionally matched from the
# acting Slack user to a Compass seat via `subscriptions.slack_user_id` — an unmapped or
# unauthorized click gets an ephemeral "you don't have access" and changes nothing.
# SLACK_SIGNING_SECRET=

# ---------------------------------------------------------------------------
# Feedback links in the daily email.
# ---------------------------------------------------------------------------

# The HMAC secret the email's one-click feedback links are signed under. Any long random
# string; `openssl rand -base64 48` is fine.
#
# Optional, and its absence is a *supported* state rather than a misconfiguration: the email
# then carries the whole report with no action links, and the report page's own buttons still
# work. Compass will not fall back to a default key — an HMAC under a known secret is not a
# signature, and the link would let anybody dismiss anybody's risk.
#
# Each link is scoped to one item and one action, expires after 30 days, is single-use for
# anything that changes state, and never establishes a session.
# COMPASS_FEEDBACK_LINK_SECRET=

# ---------------------------------------------------------------------------
# Inbound webhooks. See docs/ENGINEERING.md § Inbound webhooks.
#
# All three are optional and all three fail *closed*: with a secret unset,
# `POST /api/webhooks/<provider>` answers 401 to every delivery and logs which variable to
# set, rather than trusting a request it cannot verify. A security control that failed open
# on a misconfiguration would look exactly like one that was working.
#
# A verified delivery is acknowledged with 202 and nothing more. Compass reads its sources by
# pulling a time window through the connector port every 15 minutes, so a webhook is a hint
# that something changed and never a record — acting on the payload would let whoever holds
# the signing secret write the knowledge model.
# ---------------------------------------------------------------------------

# GitHub webhook secret. Set the same value in the repository or organization webhook
# settings; Compass verifies `X-Hub-Signature-256` over the raw body.
# GITHUB_WEBHOOK_SECRET=

# Jira webhook secret. Verifies `X-Atlassian-Webhook-Signature` for a Jira Cloud webhook
# configured with a secret, or an Atlassian Connect `Authorization: JWT` token signed with
# the shared secret. A JWT is additionally held to a 5-minute window on its `iat` rather than
# to its own `exp`: a token minted with a distant expiry is replayable until then, and the
# receiver is the only party who can refuse that.
# JIRA_WEBHOOK_SECRET=

# (Slack reuses SLACK_SIGNING_SECRET above — one secret, one verifier, so the interactivity
# endpoint and the webhook endpoint cannot disagree about what a valid Slack request is.)

# ---------------------------------------------------------------------------
# Connecting a data source. See docs/ENGINEERING.md § Public routes.
#
# All six are optional and their absence is a *supported* state. `/connect` states which
# variable a provider is missing and offers no button for it, rather than presenting one that
# 503s — and the report, delivery and the whole seeded demonstration path are unaffected. A
# deployment can therefore run, be read, and be evaluated with none of this set.
#
# Compass reads; it never writes. Every scope requested is read-only, the requested set is
# declared once per provider in its connector package, and the connect screen renders that
# same array — so the screen and the request cannot drift apart. The forbidden sets are
# asserted rather than merely omitted; see each package's `scopes.ts`.
#
# COMPASS_KMS_MASTER_KEY below is required before any of these can complete: Compass refuses
# to hold an OAuth credential it cannot encrypt, and the callback says so rather than storing
# one in the clear.
# ---------------------------------------------------------------------------

# The CSRF secret for an install round-trip. **Required by all three providers**, because a
# provider's callback arrives as a top-level navigation with no cookie — so the organization
# it belongs to cannot come from a session and has to travel in the URL. This key signs it,
# together with the provider flow it was minted for, inside a ten-minute window.
#
# There is deliberately no default: an HMAC under a key committed to the repository is not a
# signature, so with this unset every callback is refused rather than trusted. Generate with
# `openssl rand -hex 32`. Set the same value on the web and worker processes.
# COMPASS_CONNECT_STATE_SECRET=

# The GitHub App's slug, as it appears in its install URL. The App itself requests
# contents:read, pull_requests:read, issues:read, metadata:read and members:read — no write,
# no administration, no workflow, no webhook management.
# COMPASS_GITHUB_APP_SLUG=

# Jira Cloud OAuth 2.0 (3LO) client credentials. The redirect URI to register is
# `<COMPASS_BASE_URL>/api/connect/jira/callback`, byte for byte — Atlassian compares it
# against the authorize request and refuses the exchange on any difference.
#
# Requested scopes: read:jira-work, read:jira-user, read:board-scope:jira-software,
# read:sprint:jira-software, plus offline_access. The last one is not a permission over
# anybody's data — it grants a refresh token so a connection survives the one-hour access
# lifetime — and the connect screen states it apart from the other four for that reason.
# COMPASS_JIRA_CLIENT_ID=
# COMPASS_JIRA_CLIENT_SECRET=

# Slack app credentials. The redirect URI to register is
# `<COMPASS_BASE_URL>/api/connect/slack/callback`.
#
# Requested **bot** scopes, and only these two: channels:history and users:read. Not a user
# token, which would read everything the installing person can see including their direct
# messages; and deliberately not channels:read, which would let Compass enumerate the
# workspace's channels — the unscoped read the chat connector exists to make impossible. A
# manager names the channels by pasting Slack's own "Copy link", so Compass only ever reads
# the conversations it was pointed at.
# COMPASS_SLACK_CLIENT_ID=
# COMPASS_SLACK_CLIENT_SECRET=

# ---------------------------------------------------------------------------
# Encryption for integration credentials.
#
# Required before Compass will store an OAuth access or refresh token *at all*. Access and
# refresh tokens are the only secrets Compass has to be able to **use** rather than merely
# recognise, so they cannot be hashed like a session cookie or a mailed link — they are
# encrypted instead, with a per-organization data key that this key wraps.
#
# There is deliberately no default. A fallback key committed to the repository would be
# indistinguishable from storing the tokens in plaintext, so `resolveMasterKey` throws and
# names this variable instead. Nothing else in Compass needs it: the seeded connector holds
# no credentials, so a cold start and the whole demonstration path work without it.
#
# Generate with `openssl rand -base64 32` — exactly 32 bytes, base64, 44 characters. Set the
# same value on the web and worker processes. Rotating it rewraps one small row per
# organization and re-encrypts no ciphertext; see docs/ENGINEERING.md § Integration tokens.
# ---------------------------------------------------------------------------
# COMPASS_KMS_MASTER_KEY=

# ---------------------------------------------------------------------------
# Billing. See docs/BILLING.md.
#
# All five are optional and their absence is a *supported* state, not a misconfiguration. With no
# STRIPE_SECRET_KEY, `/billing` says "billing is not configured" and names the variable, `/pricing`
# still renders the whole plan table — the prices are constants in code, only the price *ids* are
# configuration — and report generation, delivery and every configuration screen work exactly as they
# do on a paid deployment. Nothing 500s and nothing is hidden.
#
# TEST versus LIVE is derived from the key prefix and shown on the billing page. `sk_live_` (or
# `rk_live_`) is live; anything else, including a malformed key, is reported as TEST. The default runs
# the other way from the usual instinct on purpose: a typo must not present a test deployment as one
# that charges real cards.
# ---------------------------------------------------------------------------

# The Stripe secret key. `sk_test_…` for test mode, which is where you should start.
# STRIPE_SECRET_KEY=

# The endpoint signing secret (`whsec_…`) for the webhook you point at
# `<COMPASS_BASE_URL>/api/stripe/webhook`.
#
# Its own variable and its own failure mode: with a secret key set but this unset, checkout works and
# the webhook endpoint refuses *every* delivery with 503 rather than trusting a request it cannot
# verify. Compass will not fall back to accepting unsigned events — a security control that fails open
# on a misconfiguration looks exactly like one that is working.
#
# Verification is an HMAC-SHA256 over the *raw* request body inside a 5-minute window, and every
# `v1` signature in the header is tried so a signing-secret rotation does not drop deliveries.
# STRIPE_WEBHOOK_SECRET=

# The Stripe price id backing each plan, one per plan.
#
# A plan with no price id configured is shown on `/pricing` and cannot be checked out — the billing
# page marks it unavailable and says why, rather than opening a session that Stripe would refuse.
# Create one recurring, per-seat price per plan in the Stripe dashboard and paste the ids here; the
# amounts Compass displays come from `packages/billing/src/plans.ts` and must match what you set.
# STRIPE_PRICE_STARTER=
# STRIPE_PRICE_TEAM=
# STRIPE_PRICE_BUSINESS=

# ---------------------------------------------------------------------------
# Single sign-on, and enterprise identity
# ---------------------------------------------------------------------------

# The CSRF secret for a sign-in round-trip. **Required before Compass offers any SSO button at
# all** — `configuredSsoProviders` returns nothing without it, so the sign-in panel shows no
# provider rather than one whose callback could not be verified.
#
# It signs `<provider>.<organization>.<nonce>.<linkUserId>.<issuedAt>`, compared in constant
# time inside a ten-minute window. Same reasoning as COMPASS_CONNECT_STATE_SECRET, and there is
# deliberately no default for the same reason: an HMAC under a key committed to the repository
# is not a signature. Generate with `openssl rand -hex 32`.
# COMPASS_SSO_STATE_SECRET=

# Google sign-in (OpenID Connect). The redirect URI to register is
# `<COMPASS_BASE_URL>/api/auth/sso/google/callback`, byte for byte — Google compares it against
# the authorize request and refuses the exchange on any difference.
#
# Requested scopes: openid, email, profile. Nothing else — no Drive, no Calendar, no Workspace
# directory. Compass reads an identity and whether Google verified the address, which is the
# fact the account-linking rule turns on.
# COMPASS_GOOGLE_CLIENT_ID=
# COMPASS_GOOGLE_CLIENT_SECRET=

# GitHub sign-in. A *separate* OAuth app from COMPASS_GITHUB_APP_SLUG above: that one reads
# code, this one only identifies a person. The redirect URI to register is
# `<COMPASS_BASE_URL>/api/auth/sso/github/callback`.
#
# Requested scopes: read:user, user:email. The second is not padding — `/user` returns a public
# profile address with no verification state at all, so `/user/emails` is the only way to learn
# whether an address is confirmed, and an unconfirmed one is never auto-linked.
# COMPASS_GITHUB_SSO_CLIENT_ID=
# COMPASS_GITHUB_SSO_CLIENT_SECRET=

# SAML and SCIM need no environment variables. Both are configured per organization from
# /enterprise and stored in the database, because the identity provider, its certificate and its
# provisioning token are per-tenant facts rather than per-deployment ones. They are gated to the
# Business plan, which `packages/billing/src/plans.ts` declares.
#
# What SAML *does* need is COMPASS_BASE_URL above: the entity id and the ACS URL are derived
# from it and must match what the identity provider was configured with, so Compass refuses to
# generate metadata rather than guess an origin from a request header.
