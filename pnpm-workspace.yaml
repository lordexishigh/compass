packages:
  - 'packages/*'
  - 'apps/*'
  - 'tools/*'

# pnpm 10 blocks lifecycle scripts by default. These are the only packages that
# legitimately need one (native/WASM binaries).
onlyBuiltDependencies:
  - '@tailwindcss/oxide'
  - esbuild
  - sharp
  - unrs-resolver
