name: CI

on:
  push:
    branches: [master]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: lint, typecheck, test
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10.15.0

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      # Lint, typecheck and the whole suite, including the architecture gates in
      # `tools/quality-gates` and the cold-start assertions in `tools/smoke`.
      - run: pnpm run verify

      # The seed is checked in, so a regeneration that produces different bytes is
      # a determinism regression rather than a routine diff.
      - name: the checked-in seed still regenerates byte for byte
        run: |
          pnpm run seed:generate
          git diff --exit-code -- seed/

  cold-start:
    name: docker compose cold start
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10.15.0

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      # A clean checkout, one command. No `.env` is written first: the compose file
      # supplies working defaults, because a demo that needs configuration is not a
      # zero-config demo, and CI proving otherwise is the point of this job.
      - name: docker compose up
        run: docker compose up -d --build

      # The acceptance criterion, as a command. `@compass/smoke` fetches `/`, waits
      # for a report within its budget, and asserts all six section headings, the
      # fixed order, at least one source link, no login form, no setup wizard, no
      # empty state and no chart. The same function runs against the real rendered
      # page in `apps/web/tests/cold-start.test.tsx`, so a green suite and a green
      # container are checking the same thing.
      #
      # The budget is raised over the 60-second criterion for one reason: a GitHub
      # runner spends most of it on PostgreSQL's first-boot initdb and on the
      # container's own start. `pnpm smoke` reports the measured time either way, so
      # a real regression in time-to-report is still visible in the log.
      - name: fetch / and assert a full six-section report
        env:
          COMPASS_SMOKE_URL: http://localhost:3000/
          COMPASS_SMOKE_BUDGET_MS: '180000'
        run: pnpm run smoke

      # `pnpm smoke` already follows the first evidence link and insists on a page,
      # so this only adds the readiness surface, which states every capability and
      # its condition rather than implying them.
      - name: the readiness endpoint answers
        run: curl --fail --silent --show-error http://localhost:3000/api/health

      - name: container logs
        if: always()
        run: docker compose logs --no-color --tail=200

      - name: docker compose down
        if: always()
        run: docker compose down --volumes
